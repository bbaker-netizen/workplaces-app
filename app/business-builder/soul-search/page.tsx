/**
 * Retired — Soul File is removed across the app. Any stale link/bookmark
 * bounces back to the Business Builder console.
 */

import { redirect } from "next/navigation";

export default function RetiredSoulSearch() {
  redirect("/business-builder");
}
